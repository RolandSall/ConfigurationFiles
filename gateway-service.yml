spring:
  data:
    mongodb:
      database: Products
      host: mongo
      port: 27017
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://register-service:8761/eureka/
  instance:
    prefer-ip-address: true

server:
  port: 8088 

